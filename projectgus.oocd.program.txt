
Program a new binary via an already existing openocd instance, or fire up a new
one. makefile snippet....

install: $(APP).elf
# try to upload via a running openocd instance first, if that
# fails then fail over to running openocd program
	( echo "halt; flash write_image erase $(APP).elf; reset" | nc localhost
4444 ) || \
	$(OPENOCD) --command "program $(APP).elf; reset"

Sourced from: https://gist.github.com/projectgus/8dab3f891c9bd694fc5f
